# Neovim config

Personal Neovim configuration.

## Layout

- `init.lua` is the main entry point.
- `lua/config/` contains shared configuration modules.
- `lua/plugins/` contains plugin specs loaded by lazy.nvim.
- `lazy-lock.json` pins plugin versions.
